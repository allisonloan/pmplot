% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package.R
\name{.bump_for_specs}
\alias{.bump_for_specs}
\title{Compute margin bumps for stacked/outside brackets}
\usage{
.bump_for_specs(
  p,
  specs_df,
  base_pt = 40,
  step_pt = 14,
  side_col = "side",
  pos_col = "pos"
)
}
\arguments{
\item{p}{A ggplot object.}

\item{specs_df}{Data frame with at least columns \code{side}, \code{pos}.}

\item{base_pt}{Base margin (points) if any outside layer exists on a side.}

\item{step_pt}{Extra margin (points) per additional outside layer on that side.}

\item{side_col, pos_col}{Column names in \code{specs_df} for side and position.}
}
\value{
A ggplot with updated margins.
}
\description{
Inspects a specs data frame and increases margins by side based on how many
distinct outside layers are present (e.g., y > 1 for top, x > 1 for right).
}
\keyword{internal}
