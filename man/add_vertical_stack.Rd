% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package.R
\name{add_vertical_stack}
\alias{add_vertical_stack}
\title{Stack multiple vertical brackets}
\usage{
add_vertical_stack(
  p,
  labels,
  ystart,
  yend,
  x,
  side = c("right", "left"),
  base_pt = 40,
  step_pt = 14,
  ...
)
}
\arguments{
\item{p}{A ggplot.}

\item{labels}{Character vector, length N.}

\item{ystart, yend}{Numeric vectors (0–1), length N.}

\item{x}{Numeric vector (0–1; can be outside), length N.}

\item{side}{"right" or "left".}

\item{base_pt, step_pt}{Margin handling (see \code{\link{add_brackets}}).}

\item{...}{Passed to \code{\link{bracket_grob}}.}
}
\value{
A ggplot with all brackets added.
}
\description{
Adds several vertical brackets at different \code{x} positions (can be outside).
Automatically bumps left/right margins for stacked outside layers.
}
\examples{
\donttest{
p <- ggplot2::ggplot(mtcars, ggplot2::aes(mpg, wt)) + ggplot2::geom_point()
add_vertical_stack(
  p,
  labels = paste("B", 1:3),
  ystart = c(0.1, 0.4, 0.7),
  yend   = c(0.3, 0.6, 0.9),
  x      = c(1.04, 1.10, 1.18),
  side   = "right"
)
}
}
