% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package.R
\name{add_many_horizontal}
\alias{add_many_horizontal}
\title{Add many horizontal brackets (auto margin by side)}
\usage{
add_many_horizontal(
  p,
  labels,
  xstart,
  xend,
  y,
  side = c("top", "bottom"),
  margin_pt = 40,
  ...
)
}
\arguments{
\item{p}{A ggplot.}

\item{labels}{Character vector.}

\item{xstart, xend}{Numeric vectors (0–1).}

\item{y}{Numeric (0–1). Can be outside (e.g., >1 for top, <0 for bottom).}

\item{side}{"top" or "bottom".}

\item{margin_pt}{Minimum margin (points) on the relevant side. Default \code{40}.}

\item{...}{Passed to \code{\link{bracket_grob}} (e.g., \code{lwd}, \code{cex}, \code{label_offset}).}
}
\value{
A ggplot with multiple brackets.
}
\description{
Vectorized helper. Pass equal-length \code{labels}, \code{xstart}, \code{xend};
a single \code{y} shared by all; choose \code{side = "top"} or \code{"bottom"}.
}
\examples{
\donttest{
p <- ggplot2::ggplot(mtcars, ggplot2::aes(mpg, wt)) + ggplot2::geom_point()
add_many_horizontal(p, c("A","B"), c(0.1,0.55), c(0.45,0.9), 1.05, side = "top")
}
}
