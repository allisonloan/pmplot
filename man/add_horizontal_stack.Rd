% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package.R
\name{add_horizontal_stack}
\alias{add_horizontal_stack}
\title{Stack multiple horizontal brackets}
\usage{
add_horizontal_stack(
  p,
  labels,
  xstart,
  xend,
  y,
  side = c("top", "bottom"),
  base_pt = 40,
  step_pt = 14,
  ...
)
}
\arguments{
\item{p}{A ggplot.}

\item{labels}{Character vector, length N.}

\item{xstart, xend}{Numeric vectors (0–1), length N.}

\item{y}{Numeric vector (0–1; can be outside), length N.}

\item{side}{"top" or "bottom".}

\item{base_pt, step_pt}{Margin handling (see \code{\link{add_brackets}}).}

\item{...}{Passed to \code{\link{bracket_grob}}.}
}
\value{
A ggplot with all brackets added.
}
\description{
Adds several horizontal brackets at different \code{y} positions (can be outside).
Automatically bumps top/bottom margins for stacked outside layers.
}
\examples{
\donttest{
p <- ggplot2::ggplot(mtcars, ggplot2::aes(mpg, wt)) + ggplot2::geom_point()
add_horizontal_stack(
  p,
  labels = c("A","B"),
  xstart = c(0.1, 0.55),
  xend   = c(0.45, 0.9),
  y      = c(1.05, 1.13),
  side   = "top"
)
}
}
