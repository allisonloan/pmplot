% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package.R
\name{add_many_vertical}
\alias{add_many_vertical}
\title{Add many vertical brackets (auto margin by side)}
\usage{
add_many_vertical(
  p,
  labels,
  ystart,
  yend,
  x,
  side = c("right", "left"),
  margin_pt = 40,
  ...
)
}
\arguments{
\item{p}{A ggplot.}

\item{labels}{Character vector.}

\item{ystart, yend}{Numeric vectors (0–1).}

\item{x}{Numeric (0–1). Can be outside (e.g., >1 for right, <0 for left).}

\item{side}{"right" or "left".}

\item{margin_pt}{Minimum margin (points) on the relevant side. Default \code{40}.}

\item{...}{Passed to \code{\link{bracket_grob}}.}
}
\value{
A ggplot with multiple brackets.
}
\description{
Vectorized helper. Pass equal-length \code{labels}, \code{ystart}, \code{yend};
a single \code{x} shared by all; choose \code{side = "right"} or \code{"left"}.
}
\examples{
\donttest{
p <- ggplot2::ggplot(mtcars, ggplot2::aes(mpg, wt)) + ggplot2::geom_point()
add_many_vertical(p, c("L","H"), c(0.1,0.55), c(0.45,0.9), 1.1, side = "right")
}
}
