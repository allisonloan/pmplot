% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package.R
\name{add_brackets_simple}
\alias{add_brackets_simple}
\alias{add_horizontal_top}
\alias{add_horizontal_bot}
\alias{add_vertical_right}
\alias{add_vertical_left}
\title{Add brackets on one side with vectorized inputs.}
\usage{
add_brackets_simple(
  p,
  side = c("top", "bottom", "left", "right"),
  labels,
  start,
  end,
  pos,
  margin_pt = 60,
  ...
)

add_horizontal_top(p, label, xstart, xend, y, margin_pt = 60, ...)

add_horizontal_bot(p, label, xstart, xend, y, margin_pt = 60, ...)

add_vertical_right(p, label, ystart, yend, x, margin_pt = 60, ...)

add_vertical_left(p, label, ystart, yend, x, margin_pt = 60, ...)
}
\arguments{
\item{p}{ggplot object}

\item{side}{One of "top","bottom","left","right"}

\item{labels}{Character vector (length 1 ok)}

\item{start, end}{Numeric 0–1 (vectors or scalars)}

\item{pos}{Numeric 0–1 (or outside) where the bracket line sits}

\item{margin_pt}{Base margin to add on the side (default 60)}

\item{...}{Passed to \code{bracket_grob()} (e.g., label_size, fontface, family, cap, label_offset, lwd)}
}
\description{
This replaces \code{add_single()}, \code{add_many()}, \code{add_many_horizontal()}, and \code{add_many_vertical()}.
Pass scalars or vectors for labels/start/end/pos. Scalars will be recycled.
}
\section{Functions}{
\itemize{
\item \code{add_horizontal_top()}: Convenience alias for top horizontal brackets

\item \code{add_horizontal_bot()}: Convenience alias for bottom horizontal brackets

\item \code{add_vertical_right()}: Convenience alias for right vertical brackets

\item \code{add_vertical_left()}: Convenience alias for left vertical brackets

}}
